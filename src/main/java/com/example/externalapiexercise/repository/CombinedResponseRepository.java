package com.example.externalapiexercise.repository;

import com.example.externalapiexercise.dto.CombinedResponse;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CombinedResponseRepository extends JpaRepository<CombinedResponse, String> {
}
